# Copy to .env and restart the Next.js server. Never commit .env.
# Optional: enables query rewrite, LLM rerank, grounded synthesis for /ask,
# and OpenAI embeddings when re-running: python3 platform/ingest/stanford/ingest.py

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# ASK_LLM_ENABLED=true
