# Curated TLE collections snapshot

**Fetched:** 2026-07-13 (from `GET /api/tle/` and `?search=ISS`).  
Trimmed for labs—not a full catalog dump (`totalItems` ~25k).

## Highlight satellites (default collection page)

| satelliteId | name |
|-------------|------|
| 25544 | ISS (ZARYA) |
| 48274 | CSS (TIANHE) |
| 20580 | HST |
| 25994 | TERRA |
| 27424 | AQUA |
| 43013 | NOAA 20 (JPSS-1) |
| 37849 | SUOMI NPP |
| 42063 | SENTINEL-2B |

## ISS search (`?search=ISS`)

Useful NORAD ids for FS2: **25544** (ZARYA), related ISS modules/debris as returned by search.

Labs should prefer `25544` as the canonical ISS track demo.
